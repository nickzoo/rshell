( Overview )
rshell is a command line program that executes other programs from input using the fork, execvp, and wait system calls. Modeled after bash, it supports arguments, comments, quotation marks (single '' and double ""), an escape character (\), and the connectors ";", "||", and "&&".

( Instructions )
From command line:
git clone https://github.com/nickzoo/rshell.git
cd rshell
make
bin/rshell

( Files )
LICENSE
Makefile
README
src/
	rshell.cpp
tests/
	exec.script
	in

( Bugs )
The cd command does nothing, and does not print an error.

The login and hostname must not exceed 255 characters.
